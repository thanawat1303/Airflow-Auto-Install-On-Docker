services:
  container-server :
    build : ./docker-server/.
    volumes :
      - ./pipe:/pipe
    privileged : true
    restart : always
    networks:
      - airflow-rtn-network

networks:
  airflow-rtn-network: