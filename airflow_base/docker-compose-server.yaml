services:
  container-server :
    build : ./docker-server/.
    volumes :
      - ./dags/pipeline:/pipeline
    privileged : true
    restart : always
    networks:
      - airflow-rtn-network

networks:
  airflow-rtn-network: